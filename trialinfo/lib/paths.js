// 이 사이트는 도메인 루트가 아니라 /trialinfo/ 아래에 보관된다. 루트는 새 사이트 몫이다.
// Eleventy는 이 디렉터리(trialinfo/)를 작업 디렉터리로 실행되며,
// 결과물은 저장소 루트의 _site/trialinfo/ 에 쓴다.
export const PATH_PREFIX = "/trialinfo/";
export const OUTPUT_DIR = "../_site/trialinfo";
