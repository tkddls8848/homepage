---
layout: layouts/page.njk
section: blog
eyebrow: Tech Blog
cta: false
---

<article class="post">
  <p class="post__meta">
    <time datetime="{{ page.date | isoDate }}">{{ page.date | postDate }}</time>
    {%- if topics %}
    {%- for tag in topics %}<span class="post__tag">{{ tag }}</span>{% endfor %}
    {%- endif %}
  </p>

  {%- if aiDraft %}
  <p class="post__notice">
    이 글은 AI(IBM Granite)가 작성한 초안을 담당자가 검토·수정해 발행한 것입니다.
  </p>
  {%- endif %}

  <div class="post__body prose">
    {{ content | safe }}
  </div>

  {%- if sources and sources.length %}
  <aside class="post__sources">
    <h2>참고한 기사</h2>
    <ul>
      {%- for source in sources %}
      <li>
        <a href="{{ source.url }}" target="_blank" rel="noopener noreferrer">{{ source.title }}</a>
        {%- if source.publisher %} <span>— {{ source.publisher }}</span>{% endif %}
      </li>
      {%- endfor %}
    </ul>
  </aside>
  {%- endif %}

  <p class="post__back"><a href="{{ '/blog/' }}">← 전체 글 목록</a></p>
</article>
